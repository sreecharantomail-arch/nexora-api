"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const express_1 = __importDefault(require("express"));
// import { getUserProfile, updateProfile, followUser, unfollowUser, getUserVideos, blockUser, unblockUser } from '../controllers/userController';
const router = express_1.default.Router();
// router.get('/:username', protect, getUserProfile);
// router.patch('/me', protect, updateProfile);
// router.post('/:id/follow', protect, followUser);
// router.delete('/:id/follow', protect, unfollowUser);
// router.get('/:username/videos', protect, getUserVideos);
// router.post('/:id/block', protect, blockUser);
// router.delete('/:id/block', protect, unblockUser);
exports.default = router;
//# sourceMappingURL=userRoutes.js.map